{{#if texture.texture}}
<img class="mc-item" data-id="{{item.id}}" src="{{texture.texture}}" alt="{{texture.name}}" title="{{item.displayName}}" data-toggle="tooltip" data-placement="top" title="{{item.displayName}}" />
{{/if}}